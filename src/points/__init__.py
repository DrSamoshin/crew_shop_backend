"""Business locations (coffeeshops, warehouses, roasteries).

Minimal model so order pickup can reference a real ``Point`` (FK + validation). The
pickup-points feature later adds repositories, services and a public/admin API.
"""
